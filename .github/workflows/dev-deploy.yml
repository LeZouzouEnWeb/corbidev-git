name: 🚀 Deploy DEV (IONOS SFTP)

on:
  pull_request:
    branches: [ develop ]
    types: [ closed ]
  push:
    branches: [ develop ]   # supprime ce bloc si tu ne veux déclencher qu’après merge

concurrency:
  group: deploy-develop
  cancel-in-progress: true

jobs:
  deploy:
    if: >
      github.event_name == 'push' ||
      (github.event_name == 'pull_request' && github.event.pull_request.merged == true)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 1 }

      # Charge la clé dans l’agent (pour nos commandes ssh)
      - name: 🔐 Setup SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: 🔧 Construire le chemin distant normalisé
        id: build_path
        env:
          REMOTE_CHEMIN: ${{ secrets.REMOTE_CHEMIN }}
          ADRESSE_GLOBAL: ${{ vars.ADRESSE_GLOBAL }}
          ADRESSE_LOCAL: ${{ vars.ADRESSE_LOCAL }}
        run: |
          set -euo pipefail
          if [ -z "${REMOTE_CHEMIN:-}" ] || [ -z "${ADRESSE_GLOBAL:-}" ] || [ -z "${ADRESSE_LOCAL:-}" ]; then
            echo "::error title=Variables manquantes::REMOTE_CHEMIN (Secret), ADRESSE_GLOBAL et ADRESSE_LOCAL (Variables) sont requis."
            exit 1
          fi
          if [ "${REMOTE_CHEMIN}" = "/" ]; then
            echo "::error title=REMOTE_CHEMIN invalide::REMOTE_CHEMIN ne peut pas être '/'."
            exit 1
          fi
          REMOTE_PATH="${REMOTE_CHEMIN%/}/${ADRESSE_GLOBAL%/}/dev/${ADRESSE_LOCAL#/}"
          REMOTE_PATH="$(printf '%s' "$REMOTE_PATH" | sed -E 's:/+:/:g')"
          echo "REMOTE_PATH=$REMOTE_PATH" >> "$GITHUB_OUTPUT"

      - name: 🔌 Tester la connexion SSH
        run: |
          ssh -o BatchMode=yes -o StrictHostKeyChecking=no \
            ${{ secrets.SFTP_USER }}@${{ secrets.SFTP_HOST }} \
            "echo 'SSH OK: ' \$(hostname)"

      - name: 📁 S'assurer que le dossier existe
        run: |
          REMOTE_PATH="${{ steps.build_path.outputs.REMOTE_PATH }}"
          ssh -o BatchMode=yes -o StrictHostKeyChecking=no \
            ${{ secrets.SFTP_USER }}@${{ secrets.SFTP_HOST }} \
            "mkdir -p -- \"$REMOTE_PATH\" && echo '✅ Dossier prêt: ' \"$REMOTE_PATH\""

      # ⚠️ Normalise la clé pour wlixcc (remplace \n par vrais retours ligne)
      - name: 🔑 Normaliser la clé privée pour SFTP action
        id: fixkey
        shell: bash
        run: |
          {
            echo "ssh_key<<'EOF'"
            printf '%s' "${{ secrets.SSH_PRIVATE_KEY }}" | sed 's/\\n/\n/g'
            echo
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: 🚚 Deploy via SFTP (incremental)
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          server: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USER }}
          ssh_private_key: ${{ steps.fixkey.outputs.ssh_key }}
          local_path: .
          remote_path: ${{ steps.build_path.outputs.REMOTE_PATH }}
          sftpArgs: "-rz --exclude='.git*' --exclude='.github/**' --exclude='node_modules/**' --exclude='.env*' --exclude='tests/**' --exclude='coverage/**'"

      - name: 👀 Lister le dossier déployé (debug)
        run: |
          ssh -o BatchMode=yes -o StrictHostKeyChecking=no \
            ${{ secrets.SFTP_USER }}@${{ secrets.SFTP_HOST }} \
            "ls -la '${{ steps.build_path.outputs.REMOTE_PATH }}' || true"
